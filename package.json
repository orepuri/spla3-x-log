{
  "name": "spla-x-match-tracker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "test:e2e": "playwright test",
    "test:e2e:update": "playwright test --update-snapshots"
  },
  "dependencies": {
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0"
  }
}
